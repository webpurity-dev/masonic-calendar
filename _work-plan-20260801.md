# Work Plan - Multi List Officers (Craft + Royal Arch)

## Configuration Strategy
- Add a section-level selector `officers_config_key` in `master_v1.yaml` for `list_officers` sections.
- Keep existing `order_regional_officers` support unchanged for backward compatibility.
- Add optional keyed configs under `order_regional_officer_sets` in Craft and Royal Arch data sources.
- Renderer resolution order:
  1. If `officers_config_key` is set and `order_regional_officer_sets` has a match, use that config.
  2. Otherwise fallback to existing `order_regional_officers`.

Approval status: ✅ Approved by user to proceed with Craft and Royal Arch changes.

## Files Affected
- `src/MasonicCalendar.Core/Loaders/DocumentLayoutLoader.cs`
  - Add `SectionConfig.OfficersConfigKey`.
  - Add `DataSourceMapping.OrderRegionalOfficerSets`.
- `src/MasonicCalendar.Core/Renderers/SectionRenderers/ProvincialOfficersSectionRenderer.cs`
  - Resolve selected officers config by key with fallback.
- `document/master_v1.yaml`
  - Set `officers_config_key` on Craft and RA list_officers sections.
  - Add RA promotions and appointments sections.
- `document/data_sources/craft_data_source.yaml`
  - Add `order_regional_officer_sets` entries: provincial/promotions/appointments.
- `document/data_sources/royalarch_data_source.yaml`
  - Add `order_regional_officer_sets` entries: provincial/promotions/appointments.

## Scope
- Implement only Craft + Royal Arch for keyed multi-source officers pages.
- Preserve behavior for all other order data sources.

## Approach
1. Add model/config properties and compile-safe fallback logic.
2. Update renderer selection logic for keyed config.
3. Update master and two data source YAML files.
4. Run targeted section renders for Craft and RA to verify no regressions.

## Risks
- YAML key typos could cause fallback to legacy source unexpectedly.
- Missing CSV files for promotions/appointments can render empty lists.

## Rollback
- Revert five changed files in one commit if needed.
- Fallback path remains active, reducing runtime risk.
