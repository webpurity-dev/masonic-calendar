# Work Plan - Conditional Other Province Rank Cleanup

## Configuration Strategy
- Use the existing YAML field mapping for `OpPastActive` already added to the relevant membership sections.
- Do not hardcode any order-specific labels or values beyond the requested `Y` / `N` branching rule.
- Keep the existing `fix_pp_abbreviations` behavior for other rank fields, but apply a separate conditional cleanup path for `ProvRankOtherProv`.

Approval status: ⏳ Awaiting user approval for 3-file implementation.

## Files Affected
- `src/MasonicCalendar.Core/Domain/SchemaUnit.cs`
  - Add `OpPastActive` to past heads, joining past heads, and honorary member models so the flag reaches rendering.
- `src/MasonicCalendar.Core/Loaders/SchemaDataLoader.cs`
  - Read `OpPastActive` from the YAML-mapped field and populate the domain models.
- `src/MasonicCalendar.Core/Renderers/Utilities/UnitModelBuilder.cs`
  - Apply the conditional `ProvRankOtherProv` cleanup when building display ranks: keep `PProv...` when `OpPastActive == "Y"`; convert `PProv` to `PPr` when `OpPastActive == "N"`.

## Scope
- Change only the three affected member categories: past heads, joining past heads, and honorary members.
- Leave officer/member paths unchanged.

## Approach
1. Add the new flag property to the three relevant domain classes.
2. Populate that property in the data loader from the existing YAML field map.
3. Add a context-aware other-province rank fix in model building so display output follows the new `Y` / `N` rule without altering unrelated rank cleanup.
4. Run a targeted build or type check for the touched C# project.

## Risks
- If any data source omits `OpPastActive`, the cleanup must safely fall back without throwing.
- A global change to rank cleanup would risk altering non-`ProvRankOtherProv` output, so the implementation should stay field-specific.

## Rollback
- Revert the three touched source files if the rendered rank output is not correct.