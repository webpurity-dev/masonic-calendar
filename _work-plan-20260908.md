# Printing Notes Redraft Plan

## Scope

Redraft the printer-facing guidance so it is concise, current, and independent of a generated output filename. Replace the stale proof version with the current document version from YAML. Exclude bleed- and margin-overlay flags because they are internal proofing aids, not printer instructions.

## Configuration Strategy

The document version remains sourced from `document/master_v1.yaml` (`document.version`). No print values or labels will be added to application code.

## Files Affected

| File | Change | Why |
|---|---|---|
| `docs/notes-printing.md` | Redraft printer instructions; identify the proof by current version only. | Removes the generated filename and refreshes the presentation of the existing print guidance. |
| `_work-plan-20260908.md` | Record scope, source of version information, and validation. | Required written plan for this non-trivial documentation change. |

## Approach

1. Retain verified production facts: A6 trim, 3 mm bleed, alternating binding margins, crop-mark proof, clean-PDF generation, and printer preflight.
2. Replace the generated proof filename with `Version 2.4.5.1`, sourced from `document/master_v1.yaml`.
3. Organize the note as a printer-ready checklist with clear production and crop-mark proof commands.
4. Verify the redraft contains no output filename or bleed- and margin-overlay flags.

## Risks

- Printer requirements may differ for imposition, colour profile, or imposed PDF delivery. The note will continue to direct confirmation with the printer.
- The recorded 338-page proof belongs to version 2.4.5, so it will be removed rather than associated with version 2.4.5.1.

## Rollback

Restore the previous revision of the printing note from version control.

## Approval Status

Approved by user on 2026-09-08.

## Validation

- Confirmed `docs/notes-printing.md` contains no generated output filename, `-showbleed`, or `-showmargins` reference.
- Confirmed the displayed version is `2.4.5.1`, matching `document/master_v1.yaml`.